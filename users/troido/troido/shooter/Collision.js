/*

var Collision = {
	circlecircle: function(x1, y1, r1,  x2, y2, r2){
		return (Math.hypot(x1-x2, y1-y2) < (r1 + r2));
	},
	rectanglerectangle: function(r1, t1, l1, b1,  r2, t2, l2, b2){
		return !( l1>=r2 || l2>=r1 || t1>=b2 || t2>=b1 );
	},
	pointCircle: function(x1, y1,  x2, y2, r2){
		return (Math.hypot(x1-x2, y1-y2) < r2);
	},
	lineCircle: function(x1a, y1a, x1b, y1b,  x2, y2, r2){
		return */
	